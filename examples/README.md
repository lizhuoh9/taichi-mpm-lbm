# Examples

This directory contains lightweight examples for the FSI LBM-MPM project.

Current examples are import and configuration smoke checks only. They do not
run LBM, MPM, coupling, or Taichi kernels.
